[project]
name = "oficina-mecanica-db"
version = "0.1.0"
description = "Sistema de gestão de oficina mecânica com interface CLI e banco de dados PostgreSQL"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "psycopg[binary]>=3.3.3",
    "python-dotenv>=1.2.2",
    "questionary>=2.1.1",
]
